Aritméticas: sí (int). Faltan float/double por AST.
Relacionales: sí (cmp + cset).
Lógicas: sí (NOT ok, AND ok, OR con bug a corregir).
Control de flujo: if/while/for/break/continue sí, vía helpers.
Funciones: helpers existen, pero el AST no emite defs/llamadas/param passing.
Memoria/stack: sí para locales; prologue/epilogue correctos. Heap/arrays no en ARM64 (solo en intérprete).
Switch: helpers implementados, pero no integrados en codegen_ast.c.
Strings: solo literals via printf; no concatenación ni equals en AST.


aarch64-linux-gnu-as -o out_arm64.o out_arm64.s
aarch64-linux-gnu-gcc -no-pie -o out_arm64 out_arm64.o
qemu-aarch64 -L /usr/aarch64-linux-gnu ./out_arm64